export let config = {
    "token" : "",
    "prefix": "!",
    "commands": [
        "testCommand",
        "butter",
        "join",
        "disconnect",
        "jake",
        "playTheme"

    ]
}
